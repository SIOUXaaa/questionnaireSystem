export type QuestionData = {
    "level": string,
    "content": string,
    "answer": string,
}

export type UserStatus = {
    id: string,
    level: string,
    role: string,
    stockSelection: string,
    shareInfo: string,
}